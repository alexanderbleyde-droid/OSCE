import { PagePlaceholder } from "@/components/page-placeholder";

export default function AdminOverview() {
  return (
    <PagePlaceholder
      title="Overview"
      sub="Operator console & activity"
      note="Activity feed, health cards, and cohort stats arrive in Phase 1 (V3 screen 06)."
    />
  );
}
