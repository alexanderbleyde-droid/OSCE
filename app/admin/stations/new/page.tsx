import { PagePlaceholder } from "@/components/page-placeholder";

export default function NewStationPage() {
  return (
    <PagePlaceholder
      title="New station"
      sub="Author a station against the StationContent contract"
      note="The station form arrives in the next step of Phase 1A."
    />
  );
}
