import { PagePlaceholder } from "@/components/page-placeholder";

export default function AdminStationsPage() {
  return (
    <PagePlaceholder
      title="Stations"
      sub="Create, version, and publish stations"
      note="Station authoring against the StationContent contract arrives in Phase 1."
    />
  );
}
