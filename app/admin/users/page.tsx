import { PagePlaceholder } from "@/components/page-placeholder";

export default function AdminUsersPage() {
  return (
    <PagePlaceholder
      title="Users"
      sub="Cohorts, roles, and specializations"
      note="User management arrives in Phase 1 (V3 screen 08)."
    />
  );
}
