import { PagePlaceholder } from "@/components/page-placeholder";

export default function ProfilePage() {
  return (
    <PagePlaceholder
      title="Profile"
      sub="Attempts, bridges fired, and your defaults"
      note="Attempt history and profile settings arrive in Phase 1 (V3 screen 05)."
    />
  );
}
