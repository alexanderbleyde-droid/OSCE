import { PagePlaceholder } from "@/components/page-placeholder";

export default function AppDashboard() {
  return (
    <PagePlaceholder
      title="Dashboard"
      sub="Your OSCE practice at a glance"
      note="Session stats, recent attempts, and the station launcher arrive in Phase 1 (V3 screen 03)."
    />
  );
}
